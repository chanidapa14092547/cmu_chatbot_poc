# 🚀 สรุปภาพรวมและลำดับการทำงานของระบบ (Comprehensive Workflow)

นี่คือภาพรวมทั้งหมดของโปรเจกต์ตั้งแต่เริ่มต้นจนถึงแผนในอนาคต สามารถนำไปใช้เป็นแกนหลักในการทำสไลด์ หรือเป็นสคริปต์พูดพรีเซนต์ได้เลยครับ

## 📂 Phase 1: การเตรียมฐานข้อมูล (Data Preparation & Optimization)
จุดเริ่มต้นของระบบคือการนำข้อมูลดิบของมหาวิทยาลัยมาสกัดให้เป็นรูปแบบที่คอมพิวเตอร์และ AI สามารถอ่านและประมวลผลได้ (Structured Data)
*   **การสกัดข้อมูลโครงสร้างหลักสูตร (Curriculum Extraction)**
    *   นำไฟล์ **PDF** โครงสร้างหลักสูตรวิชาเอก (Data Science) ปี 2567 มาแปลงเป็นรูปแบบ **JSON**
    *   นำไฟล์ **PDF** ข้อมูลวิชาโท (Minors) มาแปลงเป็นรูปแบบ **JSON**
    *   *เหตุผล:* JSON เหมาะกับข้อมูลที่มีลำดับชั้นและหมวดหมู่ซับซ้อน เช่น การแบ่งหมวดวิชาแกน วิชาบังคับ วิชาเลือก
*   **การสกัดข้อมูลตารางสอน (Schedule Extraction & Optimization)**
    *   นำข้อมูลจากไฟล์ **Excel (.xlsx)** ที่เป็นตารางสอนของทั้งมหาวิทยาลัยมาใช้งาน
    *   **ปัญหาที่พบ:** ไฟล์มีขนาดใหญ่มาก เมื่อแปลงเป็น JSON ทั่วไป ทำให้กินโควต้าความจำของ AI (Token Limit) จนประมวลผลไม่ไหว (Context Window Error)
    *   **การแก้ปัญหา (Optimization):** เขียนสคริปต์ Python (`compress_schedule.py`) กรองเฉพาะวิชาที่เกี่ยวข้อง และแปลง/บีบอัดให้อยู่ในรูปแบบ **CSV** (ใช้สัญลักษณ์ `|` คั่น) 
    *   *ผลลัพธ์:* ประหยัดพื้นที่ Token ได้เกือบ 50% ทำให้ AI อ่านข้อมูลได้ไวและเสถียรขึ้น

## 🧠 Phase 2: โครงสร้างการทำงานของ AI (Zero-Shot RAG Architecture)
ระบบนี้ใช้สถาปัตยกรรมที่เรียกว่า Zero-Shot RAG (Retrieval-Augmented Generation) คือการแนบข้อมูลไปพร้อมกับคำสั่ง เพื่อให้ AI ตอบโดยอิงจากข้อมูลของเราเท่านั้น
*   **การตั้งค่าโมเดล (Model Setup):**
    *   ใช้โมเดล Gemini ผ่าน API (เช่น `gemini-flash-lite-latest`) โดยปรับค่าอุณหภูมิ (Temperature = 0.2) ให้อยู่ในระดับต่ำ เพื่อบังคับให้ AI ตอบอย่างมีเหตุผล ไม่แต่งเรื่องเพ้อเจ้อ (Hallucinate)
*   **การฝังข้อมูล (Context Injection):**
    *   นำไฟล์ Database ทั้ง 3 ไฟล์ (Major JSON, Minor JSON, Schedule CSV) โหลดเข้าไปฝังไว้ใน **System Prompt** เพื่อให้ AI มีความรู้เทียบเท่าสำนักทะเบียน
*   **การตั้งกฎและข้อบังคับ (Strict Rules):**
    *   สั่งให้ AI ถอดรหัสคำย่อที่นักศึกษาชอบใช้ (เช่น แมท = คณิตศาสตร์, สแตต = สถิติ)
    *   สั่งบังคับไม่ให้ AI จัดตารางเรียนทันที แต่ต้องถามกลับ 2 ข้อเสมอ คือ 1. เลือกวิชาเลือกตัวไหน? 2. มีเงื่อนไขเวลาไหม?
    *   สั่งให้ AI ดึงรายชื่อ "วิชาเลือก" ที่มีเปิดสอนจริงในเทอมนั้นๆ จาก Database ออกมาให้ผู้ใช้เลือก ไม่ใช่การยกตัวอย่างลอยๆ

## ⚙️ Phase 3: กลไกการแก้ปัญหาของ AI (Chain-of-Thought Reasoning)
หัวใจสำคัญที่ทำให้แชทบอทตัวนี้จัดตารางเรียนไม่ให้เวลาชนกันได้
*   **กระบวนการคิดแบบมนุษย์ (Think out loud):**
    *   บังคับให้ AI เขียนอธิบายกระบวนการคิดออกมาก่อนที่จะสร้างตาราง (Chain-of-Thought)
    *   **ขั้นตอนที่ AI ทำ:** 
        1. หยิบวิชาแรกมา เลือกเซคชั่น และจดเวลาไว้
        2. หยิบวิชาที่สองมา เลือกเซคชั่น แล้วเทียบกับเวลาของวิชาแรก
        3. หากเวลาซ้อนทับกัน (Clash) AI จะสั่งให้ตัวเองเปลี่ยนไปเลือกเซคชั่นถัดไป
        4. ทำซ้ำจนครบทุกวิชา และได้ตารางที่ไม่มีเวลาชนกัน
    *   *ข้อดี:* การบังคับให้ AI อธิบายความคิดทีละบรรทัด จะเพิ่มความแม่นยำในการตรรกะและการประมวลผลตัวเลขได้อย่างมหาศาล

## 🚧 Phase 4: ข้อจำกัดและอุปสรรคที่พบ (Current Limitations)
การวิเคราะห์ข้อจำกัดของเทคโนโลยีในปัจจุบันอย่างตรงไปตรงมา
*   **ข้อจำกัดด้านความจำ (Token Limitations):**
    *   แม้จะบีบอัดข้อมูลเป็น CSV แล้ว แต่ถ้าในอนาคตต้องโหลดข้อมูลของ "ทุกคณะ" เข้าไปพร้อมกัน AI จะรับไม่ไหว 
*   **อาการหลอนทางคณิตศาสตร์ (LLM Hallucinations in Math):**
    *   โมเดลภาษา (LLM) ไม่ใช่เครื่องคำนวณ แม้จะมีเทคนิค Chain-of-Thought ช่วย แต่ถ้าตารางมีความซับซ้อนสูงมากๆ (เช่น ของเด็กปี 4) AI ก็ยังมีโอกาสจัดเวลาชนกันพลาดได้
*   **ข้อมูลเป็นแบบคงที่ (Static Data):**
    *   ข้อมูลตารางเรียนมาจากไฟล์ CSV ที่โหลดเก็บไว้ ทำให้บอทไม่รู้สถานะ Real-time เช่น "เซคชั่นนี้เด็กลงเต็มแล้ว" หรือ "อาจารย์ยกเลิกคลาส"

## 🌟 Phase 5: แผนการพัฒนาต่อยอด (Future Work & Deployment)
การนำ Prototype ไปสเกลเป็นโปรดักส์ที่ใช้งานได้จริงในมหาวิทยาลัย
*   **การเชื่อมต่อแพลตฟอร์มมหาวิทยาลัย (Matthew Integration):**
    *   นำชุดข้อมูล (JSON/CSV) และ System Prompt นี้ ไปฝังเป็น Custom Knowledge Base บนแพลตฟอร์ม **Matthew (Generative AI ของ มช.)** เพื่อให้นักศึกษาล็อกอินเข้าใช้งานผ่าน CMU IT Account ได้ทันที (รักษากฎหมาย PDPA และประหยัดงบ)
*   **การพัฒนาเป็น Standalone System (หากไม่ใช้ Matthew):**
    *   **Frontend:** ทำเป็น **LINE Official Account** หรือ Web Application เพื่อความสะดวก
    *   **Backend & DB:** ย้ายไฟล์ข้อมูลไปใส่ฐานข้อมูลจริง (เช่น PostgreSQL) และเอาโค้ดไปรันบน Cloud Server 
*   **แก้ปัญหา AI หลอนเลขด้วย "Function Calling" (Agentic AI):**
    *   ยกระดับแชทบอทให้เป็น Agent โดยสร้างเครื่องมือ (Tools) ที่เขียนด้วยโค้ด Python ให้ AI เรียกใช้
    *   เมื่อ AI จะจัดตาราง จะต้องโยนตารางไปให้ Python Function คำนวณ (เพราะ Code คอมพิวเตอร์เช็คเวลาชนกันได้แม่นยำ 100%) เมื่อคำนวณผ่าน ถึงจะส่งผลลัพธ์ให้นักศึกษา
*   **การเชื่อมต่อ API แบบ Real-time:**
    *   เชื่อมต่อฐานข้อมูลโดยตรงกับสำนักทะเบียน เพื่อเช็คที่นั่งว่าง และอัปเดตตารางสอนแบบ Real-time
